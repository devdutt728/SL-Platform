## FastAPI backend base URL (NOT the Next.js port)
## Default backend runs on 8000; Next.js runs on 3000.
BACKEND_URL=http://localhost:8002
NEXT_PUBLIC_AUTH_MODE=google
NEXT_PUBLIC_BASE_PATH=/recruitment

# If running behind a tunnel/reverse-proxy, set this to the externally visible origin (scheme + host).
# Example: PUBLIC_APP_ORIGIN=https://xxxx-3000.inc1.devtunnels.ms
PUBLIC_APP_ORIGIN=
# Optional override if your Google OAuth client is configured with a different redirect URI
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/recruitment/api/auth/callback/google
# Tip: you can register BOTH redirect URIs (localhost + tunnel) in Google Cloud to support both flows.

# If your local OAuth secrets JSON has a stale `redirect_uris` list, the app will still work because Google validates
# the redirect URI server-side. To enforce matching against the JSON, set:
# STRICT_OAUTH_REDIRECT_URI_CHECK=1

# Optional path to your OAuth secrets JSON (relative to repo root)
# GOOGLE_OAUTH_SECRETS_PATH=secrets/Oauth SL_Recruitment.json

NEXT_PUBLIC_PORTAL_ORIGIN=http://localhost:3000
